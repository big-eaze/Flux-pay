import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "FluxPay — Cash to Bitcoin, Seamlessly",
  description: "Convert cash to Bitcoin instantly and route it directly to any wallet you choose. No hidden fees, institutional-grade execution, full sovereignty.",
  openGraph: {
    title: "FluxPay — Cash to Bitcoin, Seamlessly",
    description: "Convert cash to Bitcoin instantly and route it directly to the wallet of your own choosing.",
    images: [{ url: "/fluxPay.png" }],
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
